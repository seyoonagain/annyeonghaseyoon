'use client';

import { NameCard } from '@/components/home';

const Home = () => {
  return (
    <div className="flex justify-center items-center h-full">
      <NameCard />
    </div>
  );
};

export default Home;
